name: 'olist_pipeline'
version: '1.0.0'
config-version: 2

profile: 'olist_pipeline'

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
seed-paths: ["seeds"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  olist_pipeline:
    staging:
      +materialized: view
    marts:
      +materialized: table
